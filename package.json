{
  "devDependencies": {
    "@prettier/plugin-ruby": "^4.0.4",
    "concurrently": "^9.0.1"
  },
  "dependencies": {
    "@rails/actiontext": "^7.1.3-4",
    "prettier": "^3.3.3",
    "trix": "^2.1.3"
  },
  "scripts": {
    "prettier": "prettier --write '**/*.rb'",
    "erblint": "bundle exec erblint --lint-all -a",
    "lint": "concurrently \"npm run prettier\" \"npm run erblint\""
  }
}
